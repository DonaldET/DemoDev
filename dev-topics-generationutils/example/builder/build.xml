<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project basedir="." default="build" name="Demogenerator Example">
	<description>Build Example Deployment</description>

	<echo>........</echo>
	<echo>Demo Generator ANT example build example</echo>
	<echo>........</echo>

	<loadproperties srcFile="build.properties" />
	<!--
	<echoproperties prefix="ant." />
	<echo />
	<echoproperties prefix="BUILD." />
	-->
	<loadproperties srcFile="${GEN_master}" />
	<loadproperties srcFile="${GEN_override}" />

	<property name="BUILD.generator.jar" value="DemoGenerator.jar" />
	<property name="BUILD.generator.path" value="../" />
	<property name="BUILD.generator.class" value="don.demo.generator.TextSourceGeneratorRunner" />

	<target name="clean" description="Remove previously generated files">
		<echo>Cleaning target from base directory ${basedir}</echo>
		<delete dir="${BUILD.target}" />
		<mkdir dir="${BUILD.target}" />
	</target>

	<target name="init" depends="clean" description="initialize target directories">
		<echo>Copy immutable files to target directory ${BUILD.target}</echo>
		<copy includeemptydirs="false" todir="${BUILD.target}">
			<fileset dir="${BUILD.immutable}">
				<exclude name="**/*.tpl" />
			</fileset>
		</copy>
	</target>

	<target name="replaceTokensFromTemplate" description="Use ANT to replace tokens">
		<!-- Taken from https://medium.com/the-geeks-of-creately/using-ant-to-replace-template-values-during-build-4f28bd3f0334 -->
		<echo message="ANT file interpolation" />
		<!-- Copy task interpolates during copy -->
		<copy todir="${BUILD.target}" verbose="false" overwrite="true" failonerror="true">
			<fileset dir="${BUILD.src}">
				<include name="**/*.apl" />
			</fileset>

			<!-- Change target file extension -->
			<mapper type="glob" from="*.apl" to="*.txt" />

			<!-- Filter chain interpolate from properties file -->
			<filterchain>
				<expandproperties />
			</filterchain>
		</copy>
	</target>

	<target name="generateFromTemplate" description="Use Freemarker template to generate values">
		<echo message="Generate artifacts using templates and properties" />
		<!-- Sample CLI parameters: ..\DemoGenerator.jar -defaultContext file:shared_defs.properties -dstDir BUILD.target -generatedFileList provenance.txt -overrideContextList file:target_defs.properties -srcDir ${BUILD.src} -templateList provenance.tpl -->
		<java jar="${BUILD.generator.path}/${BUILD.generator.jar}" fork="true" failonerror="true" maxmemory="256m">
			<arg value="-defaultContext" />
			<arg value="${GEN_master}" />
			<arg value="-dstDir" />
			<arg value="${BUILD.target}" />
			<arg value="-generatedFileList" />
			<arg value="chef_custom_resource.rb,hive_vars.hql" />
			<arg value="-overrideContextList" />
			<arg value="${GEN_override}" />
			<arg value="-srcDir" />
			<arg value="${BUILD.src}" />
			<arg value="-templateList" />
			<arg value="chef_custom_resource.tpl,hive_vars.tpl" />
			<classpath>
				<pathelement location="${BUILD.generator.path}/${BUILD.generator.jar}" />
			</classpath>
		</java>
	</target>

	<target name="build" depends="init,replaceTokensFromTemplate,generateFromTemplate" description="orchestrate target requests">
		<echo message="build completed" />
	</target>

</project>

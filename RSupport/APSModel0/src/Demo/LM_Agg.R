# Create zero-orgin, no intercept model for simple averages that are
# computed by aggregation

source("src/modeling/Describe.R")

# Conventional aggregation computes means only
agg <- DsStoreCC(sampleData)
